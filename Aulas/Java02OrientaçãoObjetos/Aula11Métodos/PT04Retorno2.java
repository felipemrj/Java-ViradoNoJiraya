package Java02OrientaçãoObjetos.Aula11Métodos;

public class PT04Retorno2 {
    /* Não podemos chamar um método sem retorno dentro de um println
    Podemos usar "return;" como se fosse um break para métodos que retornam void
    Dessa forma podemos evitar o uso de if/else, deixando o código mais limpo
    Outros pontos sobre o uso de return em métodos:
    - O return pode ser usado para retornar imediatamente de um método, inclusive antes do fim do bloco.
    - Em métodos que retornam valores, o tipo do valor retornado deve ser compatível com o tipo declarado do método.
    - Métodos com return podem ser utilizados em expressões, atribuições e como argumentos de outros métodos.
    - O uso de múltiplos return pode simplificar o fluxo do código, evitando estruturas de controle profundas.
    - Em métodos void, o return; serve apenas para finalizar a execução antecipadamente, sem retornar valor.
    - O compilador exige que todos os caminhos possíveis em métodos com retorno garantam um valor de saída (return), exceto em métodos void.
    - O return não pode ser usado fora de métodos, construtores ou blocos de inicialização.
    - O return pode ser útil para validar condições e encerrar o método caso não sejam atendidas, melhorando a legibilidade. */

}
