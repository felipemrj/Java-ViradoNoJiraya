package eJavaClassesUtilitárias.Aula31RegularExpression;

public class PT01PatternMatcher {
    /*
    A API de Expressões Regulares do Java, presente no pacote java.util.regex, permite buscar, validar e manipular padrões
    em textos utilizando a linguagem de metacaracteres conhecida como regex (regular expressions).

    Principais classes:
    - Pattern: Representa o padrão regex compilado.
    - Matcher: Permite realizar buscas, validações e extrações no texto conforme o padrão definido.

    Como utilizar:
    - Pattern.compile(regex): Compila a expressão regular.
    - pattern.matcher(texto): Cria um Matcher para buscar o padrão no texto.
    - matcher.find(): Procura as próximas ocorrências do padrão no texto.
    - matcher.start(): Retorna o índice inicial de cada ocorrência encontrada.

    Exemplo prático:
    - Útil para validação de formatos (e-mail, CPF, telefone), extração de dados (encontrar todos os e-mails em um arquivo), busca de padrões repetidos, entre outros.
    - No exemplo acima, o padrão "ab" é procurado no texto "abababa", e são exibidas as posições onde o padrão aparece.

    Resumo:
    - Use expressões regulares para buscar, validar ou extrair padrões complexos de texto de forma eficiente.
    - A API do Java facilita o uso de regex, tornando o código mais legível e performático para operações textuais avançadas.
    */
}
