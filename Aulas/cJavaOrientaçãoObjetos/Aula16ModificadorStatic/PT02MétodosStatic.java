package cJavaOrientaçãoObjetos.Aula16ModificadorStatic;

public class PT02MétodosStatic {
/*
Métodos static pertencem à classe e não a uma instância específica, por isso não é possível usar o this dentro deles,
já que this representa o objeto criado e pode não existir no contexto static.
Para acessar atributos ou métodos static, utilizamos o nome da classe, por exemplo: NomeDaClasse.atributoStatic.
O modificador static é indicado para funcionalidades que não dependem do estado individual dos objetos, como utilitários,
 contadores globais ou constantes.
Membros static são compartilhados entre todas as instâncias da classe e podem ser acessados diretamente pela classe, sem necessidade de criar um objeto.
*/
}
